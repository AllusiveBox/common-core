/**
 *
 * Utility type that represents a number or a string that is also a number.
 *
 * @type NumericalString
 * @since Introduced in Version 0.1.0.
 *
 */
export type NumericalString = `${number}` | number;
