{
  "name": "common-core",
  "version": "0.1.0-beta",
  "description": "",
  "main": "dist/.src/index.js",
  "types": "dist/.src/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:docs": "npm run build && npm run build:nocomments && typedoc",
    "build:nocomments": "tsc -p ./tsconfig.nocomments.json",
    "build:npmrelease": "npm run build && npm run build:nocomments",
    "prepublish": "jest && npm run build:npmrelease",
    "test": "jest",
    "integration": "jest --group=Integration",
    "unit": "jest --group=Unit"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AllusiveBox/common-core.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/AllusiveBox/common-core/issues"
  },
  "homepage": "https://github.com/AllusiveBox/common-core#readme",
  "devDependencies": {
    "@types/jest": "^28.1.4",
    "@types/node": "^18.0.0",
    "jest": "^28.1.2",
    "jest-extended": "^2.0.0",
    "jest-runner-groups": "^2.2.0",
    "ts-jest": "^28.0.5",
    "typedoc": "^0.23.4",
    "typescript": "^4.7.4"
  }
}
